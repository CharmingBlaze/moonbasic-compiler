# testdata/

Small `.mb` fixtures for compiler, VM, and CI `--check` / unit tests.

Prefer adding focused scripts here over growing large examples. Showcase games live under [`examples/`](../examples/).
