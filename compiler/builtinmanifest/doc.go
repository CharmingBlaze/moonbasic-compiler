// Package builtinmanifest provides compile-time signatures for built-in dotted commands.
// It is the single source of truth for semantic type checking; runtime modules should
// register matching handlers under the same canonical uppercase names.
package builtinmanifest
