Goroutine launch audit (`go func(` in moonbasic + internal; excluding third_party except note).

moonbasic/runtime/system/host.go:216
  go func() { _ = cmd.Wait() }()
  — Waits on subprocess; terminates when command exits (no leak).

moonbasic/internal/bench/capture.go:20
  — Benchmark harness only.

vm/heap/release_once_test.go
  — Concurrent Free() test.

third_party/go-enet/test/*.go
  — Upstream ENet tests only.

No terrain async chunk-build goroutines found under `go func(` in runtime/ at time of audit (verify if streaming adds goroutines later).
