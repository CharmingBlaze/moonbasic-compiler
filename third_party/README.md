# third_party/

Vendored native / CGO dependencies (e.g. `jolt-go`, `go-enet`).

Do not casually move or rewrite these trees; build tags and linker flags depend on their layout. See [`docs/PHYSICS.md`](../docs/PHYSICS.md) for Jolt constraints.
