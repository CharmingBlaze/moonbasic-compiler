# Arena (2D “FPS” practice)

WASD moves the player square; mouse crosshair and left button register hits on drifting targets. For real 3D + `PHYSICS3D` + `NET.*`, extend this loop with `RENDER.BEGINMODE3D` / Jolt when available on your platform. Run from repo root:

```bash
moonbasic examples/fps/main.mb
```
