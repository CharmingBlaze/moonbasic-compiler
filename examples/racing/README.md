# Racing (top-down)

WASD drive a car-like pair of rectangles; touch the yellow checkpoint then the cyan one to increment `lap`. Uses `MATH.SIN` / `MATH.COS` for heading. Run from repo root:

```bash
moonbasic examples/racing/main.mb
```
