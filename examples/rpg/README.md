# Mini RPG

Top-down movement into a “forest” rectangle to accumulate `gold`; on exit, `examples/rpg/save.json` is written with `px`, `py`, and `gold` (JSON). Delete the file to reset. Run from repo root:

```bash
moonbasic examples/rpg/main.mb
```
